// 143 - first IAR project, sum of two numbers
// program adds 2 integer variables and stores the result
void main( void )
{
    int a = 5, b = 4;  // operands
    int sum = 0;       // will hold the result
    sum = a + b;       // the sum operation

    while(1)
    {
        // infinite loop so we can inspect the sum in debug mode
    }
}
