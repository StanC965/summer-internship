#include <iom324pb.h>

int main(void)
{
  
    // In fisierul task63.i se poate observa ca macro-urile au fost expandate
    // in fisierul in care a fost inclus header-ul.
    // Acest lucru imi confirma ce am inteles pana acum: macro-urile din header
    // sunt transformate in declaratii de registre hardware, reprezentate prin
    // structuri, pentru ca acestea sa poata fi folosite direct in codul C.

    while (1)
    {
    }
}