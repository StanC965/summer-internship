# Quiz 5 - Week 5 Review

<!-- markdownlint-disable MD037 MD060 -->

Select an answer for each question and press **Check answers**.

This quiz is based on Week 5 Review questions from `w5.pdf`.

<!-- markdownlint-disable-next-line MD033 -->
<div id="quiz-app" data-quiz-id="quiz-5" data-quiz-title="Quiz 5 - Week 5 Review" data-submit-endpoint=""></div>

| Question | A | B | C | D | Correct |
| --- | --- | --- | --- | --- | --- |
| Points A,B,C are MCU controlled with the following logic levels: 1, 0 and 1 respectively. What LED is active? | both | LED1 and LED2 | LED2 | LED1 | - |
| What is a time slice within the context of scheduling algorithms? | the total execution time of a schedule | the elapsed time between 2 tasks | the execution time of a task | the elapsed time between 2 consecutive timer interrupts | - |
| A 10-bit resolution ADC has a voltage reference range of 3.3V. What is the conversion step size in mV? | 3.22 mV | 2.5 mV | 1.07 mV | 0.003 mV | - |
| What button de-bounce is? | an unwanted signal instability | a mechanical imperfection of the button | an electric phenomenon | a noise filter | - |
| What will be the final value of a variable in the code below: /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ #define CRC_POLYNOME(x) x\*x+x+2 void main(void){ int a = 3; a ^= CRC_POLYNOME(a+1); } /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ | 15 | 22 | 14 | 13 | - |
| What this code implements? /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ void my_func(int \*a); int x[3]; void main (void){ my_func(x); } /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ | passing by value the x array | passing by reference the x array |  |  | - |
| What would be the y final value in the code below? #define CUBE(x) x \* x \* x void main( void ){ int x = 5; int y = 0; y = CUBE(++x); } | 216 | 336 | 125 | 80 | - |
| GND means.... | 3.3V | 0 V | ESD protection | earth link | - |
| Which of the following is an advantage of using interrupts over polling in embedded systems? | faster instruction execution | reduced processor load | simpler implementation | none of the above | - |
| Which of the following best describes what is the ADC resolution? | the number of quantification levels in the process of conversion | the number of bits used to represent the analog input signal as a digital value | how fast the converter can output a data result | the maximum least significant bit error in the process of conversion | - |
| If timer TC0 is used as system ticker for the Task Scheduler, can you use TC0 for other purposes in your program? | yes, because it can be reconfigured | yes, because it has alternative functions | no, TC0 remains a dedicated timer to Task Scheduler | no, TC0 cannot trigger too many interrupt requests | - |
| What is a NULL pointer in C? | numerical value 0 | random pointer | imaginary pointer | large pointer | - |
| Is it technically valid to address an element beyond the end of an array? unsigned char v[3] = {9,8,7}; unsigned char a = 6, b = 5; a = v[3]; b = v[4]; | no, the compiler will throw an error | yes, to address it, but not to use the value there | no, my colleagues during code review will not allow this |  | - |
| What is the maximum value that TC0 can count up to in Normal Mode? | 512 | 128 | 255 | 1024 | - |
| [Select all that apply] What is the difference between signed int i and unsigned int i? | in the case of signed int i, the most significant bit becomes the sign bit | in the case of signed int i, the least significant bit becomes the sign bit | you can store more positive values with unsigned int i |  | - |
| Points A,B,C are MCU controlled with the following voltage levels: 3.3V, 0V and 3.3V respectively. What LED is active? | both | LED2 | LED1 and LED2 | LED1 | - |
| Supposing the code below: static int i=10; i=25; int main(){ int j = i++; return 0; } | variable j will have the value 11 | variable j will have the value 26 | an compilation error occurs | variable j will have the value 25 | - |
| What is a stack in computer programming? | a special function register | a peripheral of the MCU | an array in RAM | a component of the CPU | - |
| [Select all that apply] What elements make a basic task scheduler to work properly? | Tasks Dispatcher | UML Diagrams | Flags Manager | System Tick | - |
| What would be the result of executing this code? /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ int x = 0x2 << 3; /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ | 0x06 | 0x10 | 0x04 | 0x01 | - |
| Assuming you have a big endian memory. What would be the final value of x after the following code is executed? unsigned short x =0xABCD; unsigned char \*p = (unsigned char \*) &x; ++p; \*p=0xEF; | 0xCDEF | 0xEF | 0xABCD | 0xABEF | - |
| Which keyword is commonly used in C language to indicate that a function is part of the provided interface in a header file? | public | provided | static | extern | - |
| [Select all that apply] Which of the following are modes of operation for TC0? | Random Mode | Fast PWM Mode | CTC Mode | Normal Mode | - |
| When a variable is declared: | the compiler is informed about the type and size of that variable | the compiler is informed about the type and size of that variable and also the necessary space is reserved in memory. |  |  | - |
| [Select all that apply] Which of the following describe the interrupt service routine? | it is not called directly from the main program | it has no return values | it has no input parameters | it should contain short code | - |
| What is the effect of setting the Clock Select (CS) bits to 000 on TC0 in CTC Mode? | TC0 will count up at a rate of 1/64 of the system clock frequency. | TC0 will count up at a rate of 1/256 of the system clock frequency. | TC0 will stop counting and remain at its current value. | TC0 will count up at a rate of 1/1024 of the system clock frequency. | - |
