# Quiz 6 - Week 3 Goal 6

Select an answer for each question and press **Check answers**.

This quiz is based on Week 6 Review questions from `w6.pdf`.

<!-- markdownlint-disable-next-line MD033 -->
<div id="quiz-app" data-quiz-id="quiz-6" data-quiz-title="Quiz 6 - Week 3 Goal 6" data-submit-endpoint=""></div>

| Question | A | B | C | D | Correct |
| --- | --- | --- | --- | --- | --- |
| What does this line implement: `short internship @ 0x000121`? | Defines an 8bit variable at address `0x000121` | Declares a non-initialized integer variable named `internship` | Declares a 16bit variable at address `0x000121` | Defines a 16bit variable at address `0x000121` | D |
| VCC means... | 5V | ESD protection | 0V | Power input level, depending on circuit design | D |
| In this snippet (`#define VARIANTS 3` and `#ifdef VARIANT ... #else ... #endif`), which variable gets compiled? | y | x | none, as VAR does not have any value | x, because VAR is assumed to be 0 by the preprocessor | A |
| What is a bitmask in C language? | A set of bits used to mark the bit positions you want to modify with binary operators | A way of accessing bits from right to left | A function that returns the specific bit position in an arithmetic operation | An array of boolean values | A |
| What is the final value of `b` in `#define SQUARE(x) x*x` with `int a = 3; int b = SQUARE(a+1);`? | 7 | 16 | 49 | 11 | A |
