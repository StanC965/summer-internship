# Quiz 5 Data - Week 3 Goal 5

| Question | A | B | C | Correct |
| --- | --- | --- | --- | --- |
| A task scheduler is mainly about managing what shared resource? | Flash memory pages | CPU time across functions | UART baud-rate slots | B |
| Which three building blocks are listed at the core of the scheduler? | Parser, linker, loader | System tick, flags manager, tasks dispatcher | ADC, PWM, USART | B |
| In the dispatcher, what should happen after calling a task whose flag was set? | Reset that task flag | Disable global interrupts forever | Reinitialize all modules | A |
| What function role does `scheduler_cfg.c` play after refactor in Objective 2? | Hardware register map copy | Holds task callback bodies/connections | Stores compiler options | B |
| Debounce in Objective 3 is based on what principle for 5 samples? | Average voltage threshold only | Majority consistency (`00000` or `11111`) | First sample wins | B |
