# Quiz 7 - Week 3 Goal 7

Select an answer for each question and press **Check answers**.

This quiz is based on Week 3 Goal 7 work instructions.

<!-- markdownlint-disable-next-line MD033 -->
<div id="quiz-app" data-quiz-id="quiz-7" data-quiz-title="Quiz 7 - Week 3 Goal 7" data-submit-endpoint=""></div>

| Question | A | B | C | Correct |
| --- | --- | --- | --- | --- |
| For this MCU peripheral, the goal notes that naming is USART instead of UART mainly because it can also support what? | Synchronous mode using a clock line | Ethernet framing | CAN-FD tunneling | A |
| The IO1 jumper connecting TX and RX on the same peripheral is used to simulate what setup? | Dual-core SPI DMA | Loopback for simple one-way communication test | I2C multi-master arbitration | B |
| Objective 3 starts UART driver implementation with what first step? | Full protocol stack and CRC | Send and receive a single byte, then extend | LIN diagnostics only | B |
| In the Control Panel refactor, virtual MCU1 primarily does what before sending UART commands? | Executes LED hardware directly only | Reads and interprets buttons and sends LED behavior commands | Disables all timers | B |
| In this simplex model, virtual MCU2 mainly does what? | Generates sensor ADC values | Receives UART data and executes LED behavior | Programs fuses | B |
