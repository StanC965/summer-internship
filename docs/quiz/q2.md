# Quiz 2 - Week 2 Review

<!-- markdownlint-disable MD037 MD060 -->

Select an answer for each question and press **Check answers**.

This quiz is based on Week 2 Review questions from `w2.pdf`.

<!-- markdownlint-disable-next-line MD033 -->
<div id="quiz-app" data-quiz-id="quiz-2" data-quiz-title="Quiz 2 - Week 2 Review" data-submit-endpoint=""></div>

| Question | A | B | C | D | Correct |
| --- | --- | --- | --- | --- | --- |
| What would be the DDRD result of executing this code? /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ DDRD = 0x0F; #define REGC 0x03 DDRD &= REGC; /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ | 0x03 | 0x0C | 0x00 | 0x0F | - |
| What implements the following line of code: /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ #define too unsigned too short internship @0x000234 /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ | defines a 8bit variable at address 0x000234 | defines a 16bit variable at address 0x000234 | declares a 16bit variable at address 0x00234 | defines a 32bit variable at address 0x000234 | - |
| What would be the disadvantage of using polling mechanism in your embedded program? | processor will execute instructions with no real use | program will have latency in response, processor will be highly loaded | program will look like spaghetti code, old and rusted | processor is interrupted from its main execution flow | - |
| How you will store in a little endian architecture the following unsigned long variable 0x87 65 43 21? | 12 34 56 78 | 21 43 65 87 | 43 21 87 65 | 87 65 43 21 | - |
| Assume A, B, C points are connected to 3 microcontroller individual pins. What LED is turned ON if you apply 1 logic in point A, 0 logic in point B and 1 logic in point C? | none | both | LED1 | LED2 | - |
| What would be the result of executing this code? /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ int x = 0x3 << 2; /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ | 0x30 | 0xC0 | 0x0C | 0x06 | - |
| Which register bit is capturing the state of a button connected to port B, pin 3? | port_bit3 | PORTB3 | DDRB3 | PINB3 | - |
| Is it a good idea to include .c files within .c files as alternative to include .h files within .c files? | yes, because otherwise the program wont work | no, because you will loose the advantage of modularity; compiler will output one huge object file | yes, the compiler allows that because it facilitates faster access to the functions code. | no, because all private functions will become public functions | - |
| Assume you have a 32bit variable var initialized with value 1 and you want to set the four least significant bits to 1010. Which of the following is ok? | var = (var & 0xFFFFFFF) \| 0xA | var = (var & 0xFFFFFFF0) \| 0xA0 | var = (var & 0xFFFFFFF0) \| 0xA | var = (var & 0xFFFFFFF) \| 0xA0 | - |
| Assume A, B, C points are connected to 3 microcontroller individual pins. What LED is turned ON if you apply 0 logic in point A, 1 logic in point B and 1 logic in point C? | LED1 | LED2 | both | none | - |
| What is a bitmask in C language? | a way of accessing bits from right to left | a function that returns the specific bit position in an arithmetic operation | a set of bits used to mark the bit positions you want to modify with binary operators | an array of boolean values | - |
| Why are we using header file guardians for our modules in C language? | to prevent a header file to be included multiple times | to improve the compilation process | to prevent public access to the private functions and variables in module | to prevent compilation errors | - |
| The following code is an example of conditional compilation. What variable gets compiled? #define VAR #ifdef VAR int x; #else int y; #endif | x, because VAR it is assumed to be 0 by the preprocessor | y | x | none, as VAR does not have any value | - |
| What will be the final value of b variable in the code below: /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ #define SQUARE(x) x\*x void main(void){ int a = 3; int b = SQUARE(a+1); } /\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/ | 16 | 49 | 7 | 11 | - |
| What would be the result of this bitwise operation? 0x7F ^ 0x3F | 0x30 | 0x40 | 0xC0 | 0x7E | - |
