# Quiz 2 - Week 1 Goal 2

Select an answer for each question and press **Check answers**.

This quiz is based on Week 1 Goal 2 work instructions.

<!-- markdownlint-disable-next-line MD033 -->
<div id="quiz-app" data-quiz-id="quiz-2" data-quiz-title="Quiz 2 - Week 1 Goal 2" data-submit-endpoint=""></div>

| Question | A | B | C | Correct |
| --- | --- | --- | --- | --- |
| What loop is expected in embedded main() for continuous execution? | while(1) | for(;;i++) | do { } while(0) | A |
| In Objective 1, how is a 1 second LED delay first introduced? | Hardware watchdog reset | Software delay with a large for loop | UART timeout register | B |
| What is the main purpose of the gpio.c and gpio.h module split? | Increase compiler warnings only | Separate basic pin handling into a reusable module | Remove all header files | B |
| In Objective 5 for SW0, what should be enabled on the input pin for button reading? | Pull-up resistor | PWM output compare | ADC auto trigger | A |
| For SOS architecture, POINT and LINE behaviors are built on top of what lower-level behavior? | ADC conversion routine | LED on/off basic functions | UART receive interrupt | B |
