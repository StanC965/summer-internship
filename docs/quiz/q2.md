# Quiz 2 - Week 1 Goal 2

Select an answer for each question and press **Check answers**.

This quiz is based on Week 2 Review questions from `w2.pdf`.

<!-- markdownlint-disable-next-line MD033 -->
<div id="quiz-app" data-quiz-id="quiz-2" data-quiz-title="Quiz 2 - Week 1 Goal 2" data-submit-endpoint=""></div>

| Question | A | B | C | D | Correct |
| --- | --- | --- | --- | --- | --- |
| How will little endian store the unsigned long value `0x87 65 43 21`? | 87 65 43 21 | 21 43 65 87 | 12 34 56 78 | 43 21 87 65 | B |
| What is the final value of `b` in `#define SQUARE(x) x*x` with `int a = 3; int b = SQUARE(a+1);`? | 7 | 16 | 49 | 11 | A |
| What is a disadvantage of using polling in an embedded program? | Program will have latency in response and processor will be highly loaded | Processor is interrupted from its main execution flow | Processor will execute instructions with no real use | Program will look like spaghetti code | A |
| In conditional compilation with `#define VAR`, which variable gets compiled? | y | x | none, as `VAR` does not have any value | x, because `VAR` is assumed to be 0 by the preprocessor | B |
| Why are header file guardians used in C modules? | To prevent a header file from being included multiple times | To prevent compilation errors only | To improve the compilation process only | To prevent public access to private functions and variables in module | A |
