# Quiz 4 - Week 2 Goal 4

Select an answer for each question and press **Check answers**.

This quiz is based on Week 4 Review questions from `w4.pdf`.

<!-- markdownlint-disable-next-line MD033 -->
<div id="quiz-app" data-quiz-id="quiz-4" data-quiz-title="Quiz 4 - Week 2 Goal 4" data-submit-endpoint=""></div>

| Question | A | B | C | D | Correct |
| --- | --- | --- | --- | --- | --- |
| GND label used in electronic circuits means... | Earth link | 3.3V | ESD protection | 0V | D |
| For what purpose is `volatile` keyword used in C language? | To declare a pointer | To reserve a special area in memory for this type of variables | To prevent compiler optimizations for variables that are not changed by the program | To allow a variable to be initialized without an explicit value | C |
| What is the result of `int x = 0x3 << 2;`? | 0x06 | 0x0C | 0xC0 | 0x30 | B |
| What is a bitmask in C language? | A way of accessing bits from right to left | A set of bits used to mark the bit positions you want to modify with binary operators | A function that returns the specific bit position in an arithmetic operation | An array of boolean values | B |
| A 10-bit ADC with VREF = 3.3V has which conversion step size? | 0.003 mV | 3.22 mV | 1.07 mV | 2.5 mV | B |
