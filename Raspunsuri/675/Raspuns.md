# 675 — STRETCH — What would be the advantage of using pointers to functions within state machines? #
The advantage of pointers to functions within state machines is that it keeps the code more organized and spread into smaller parts.Also since it is just a jump in low level programming it increases execution speed
Avoiding large switch statements by using functions also can save flash memory.